# 基本パッケージ
opencv-python>=4.5.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
Pillow>=8.3.0
scipy>=1.7.0
seaborn>=0.11.0
openpyxl>=3.0.0

# セグメンテーション
torch>=2.0.0
torchvision>=0.15.0
segmentation-models-pytorch>=0.3.0
tqdm>=4.65.0

# ONNX Runtime（GPU推論用・オプション）
# Windows GPU (DirectML): pip install onnx onnxruntime-directml
# NVIDIA GPU (CUDA): pip install onnx onnxruntime-gpu
# ※ RTX 5070 Ti等の新しいGPUはonnxruntime-gpu (CUDA) を推奨
# ※ onnxruntimeとonnxruntime-directml/onnxruntime-gpuは同時にインストール不可

